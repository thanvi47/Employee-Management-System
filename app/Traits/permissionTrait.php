<?php
namespace App\Traits;
use \Illuminate\Support\Facades\Route;






trait permissionTrait
{
    public function hasPermission()
    {
//        if (!isset(auth()->user()->role->permission['name']['department']['can-view']) && \Route::is('admin.department.index')) {
//            return abort(401);
//        }
//
//        if (!isset(auth()->user()->role->permission['name']['department']['can-edit']) && \Route::is('admin.department.edit')){
//        return abort(401);
//    }
//    if (!isset(auth()->user()->role->permission['name']['department']['can-delete']) && \Route::is('admin.department.destroy')){
//        return abort(401);
//    }
//    if (!isset(auth()->user()->role->permission['name']['department']['can-add']) && \Route::is('admin.department.create')){
//        return abort(401);
//    }




    }
}
